name: Update Flex endpoint

on:
  push:
    branches:
      - main

defaults:
  run:
    shell: bash

jobs:
  call-flex-update:
    uses: symfony/recipes/.github/workflows/callable-flex-update.yml@main
    with:
      contrib: true
      versions_json: https://raw.githubusercontent.com/symfony/recipes/main/.github/versions.json
